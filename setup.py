from setuptools import setup

setup(
    description="A Python package to simplify interactions with NASA's Unity Platform.",
    install_requires=[
        'requests',
    ],
    name='unity-py',
    packages=[
        "unity_py",
    ],
    version='0.0.1',
)
